class bodacious{
	public static void main(String args[]){
		/*int j = 0;
		do for(int i = 0; i++<2;){
			System.out.println(i);
			System.out.println("j is " + j);}
		while (j++ < 2 );*/
		StringBuffer str = new StringBuffer("vijay jangir") ;
		
		
		//str = str.append(" kumar");
		//str = str.deleteCharAt(3);
		//str = str.insert(2, " + ") ;
		//str = str.delete(0,5);
		//str.setCharAt(3,'i');
		//str = str.replace(5,6, " Kumar ");
		//str = str.reverse();
		//str.trimToSize();
		//str.ensureCapacity(20);
		//System.out.println(str.length()) ;
		//System.out.println(str.capacity()) ;
		//System.out.print(str) ;
		/*int a = 3 	0,b = 20 ;
		float c = 15.0f ;
		float d = Math.max(a,c) ;*/

		/*double a = 30.6 ,b = 24.0, d;
		d = Math.ceil(a) ;  // it returns 31.0 double type value */
		
		/*double a = 30.6 ,b = 24.0, d;
		d = Math.floor(a) ;  // it returns 30.0 double type value */

		double a = 3.0 ,b = 4.0, d;
		d = Math.pow(a,b) ;  // it returns 3 power 4 = 81.0 double type value 
		//others are Math.exp(arg) = 'e' raised to the power argument
		//Math.log(arg) = return log vaue of argument
		//Math.round(arg) = roundsoff the argument nearest integer
		//Math.toRadians(arg) = 
		//Math.PI =22/7
		Math.E = 
		
		System.out.print(d);

		
	}
}